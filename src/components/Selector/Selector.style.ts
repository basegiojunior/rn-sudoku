import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '100%',
    maxHeight: 40,
  },
  scrollContainer: {},
  scrollContent: {
    alignItems: 'center',
  },
  textContainer: {
    alignItems: 'center',
  },
  text: {
    fontSize: 18,
    color: '#0f0f0f',
    fontWeight: 'bold',
  },
});

export default styles;
